# Codex 專案操作規範

## 專案說明

本專案是以 Raspberry Pi 5 開發的智慧型後方接近警示系統。

目前使用的硬體包含：

- TFMini LiDAR 距離感測器
- BNO055 慣性測量單元
- 紅色、黃色及綠色 LED
- 藍牙安全帽耳機

## 安全規範

- 僅操作目前的 Git 專案目錄。
- 修改檔案前先檢查 `git status`。
- 不得自行執行 `sudo`。
- 不得自行執行 `reboot`、`shutdown` 或破壞性指令。
- 不得刪除既有實驗程式或測試資料。
- 不得讀取或修改 SSH 私鑰、登入憑證及權杖。
- 不得將密碼、金鑰或權杖提交至 Git。

## Git 規範

- 不得在未經同意的情況下直接推送至 GitHub。
- 修改完成後先顯示 Git diff。
- 提交前必須由使用者確認。
- 使用清楚的 Conventional Commit 訊息。
- 新功能原則上使用 feature 分支開發。

## 二、硬體與系統操作規範

8. 執行任何 GPIO、UART、I2C、藍牙或 systemd 測試前，必須先向使用者說明將執行的指令、預期效果，以及可能影響硬體、通訊介面或系統服務的風險，取得使用者確認後才能執行。
9. 感測器封包解析器（Parser）不得直接依賴實體 UART；Parser 必須能接收獨立的位元組資料，並可使用模擬資料或測試資料單獨驗證。
10. UART 硬體介面、封包解析、資料濾波、警示策略及輸出裝置必須實作為不同模組，並透過明確介面協作。
11. 核心警示程式不得依賴圖形使用者介面，必須能在無桌面環境中獨立執行與測試。
12. 不得讓兩個或以上的程式同時直接開啟同一個 UART 裝置；UART 的存取權必須由單一程序或單一介面統一管理。

## 三、程式品質與驗證

13. 每次修改後，必須執行與該修改相關的測試或檢查，確認結果後再回報；若尚無適用的自動化測試，必須執行可行的靜態或文件檢查，並清楚說明未執行測試的原因與風險。
14. 所有新增程式都必須包含完整且合理的型別提示，並針對輸入無效、通訊失敗、逾時、資料不完整及其他可預期的異常提供必要的錯誤處理。
